# 4D Spring Reverb — Σχεδιαστικό Δελτίο (CVA Labs): Spring Reverb με 4D Lattice Spring Model

## 1. Σύνοψη

Το plugin προσομοιώνει spring reverb **φυσικά**: το ελατήριο είναι ένα
Lattice Spring Model (LSM) — δίκτυο σημειακών μαζών που συνδέονται με
ελατήρια Hooke — του οποίου η γεωμετρία ηρεμίας ζει σε **4 διαστάσεις**:
τρεις χωρικούς άξονες (x = κατά μήκος του ελατηρίου, y/z = εγκάρσια) και
έναν 4ο άξονα `w` («φάση περιέλιξης»). Κάθε κόμβος έχει 4 βαθμούς ελευθερίας
(x,y,z,w) και τα ελατήρια δρουν προς σταθερές διευθύνσεις στον 4D χώρο.

## 2. Γιατί 4D;

Σε πραγματικό ελικοειδές ελατήριο, η αξονική, η εγκάρσια και η στρεπτική
κίνηση είναι γεωμετρικά συζευγμένες — γι' αυτό τα spring reverbs έχουν
διασπορά, «boing» και μεταλλικό χαρακτήρα. Ενσωματώνοντας την περιέλιξη
ως 4η διάσταση και δίνοντας στα ελατήρια διευθύνσεις με συνιστώσες και
στον `w`, η ενέργεια ανταλλάσσεται συνεχώς μεταξύ των τεσσάρων καναλιών
κίνησης — ο μηχανισμός της διασποράς προκύπτει από το ίδιο το μοντέλο.

Η γεωμετρία ηρεμίας (κεντρική γραμμή, έλικα σε 4D):

    C(t) = ( L·t,  r·cos(θ),  r·sin(θ),  rW·sin(2θ) ),   θ = 2π·nCoils·t

με L = 1, r = 0.25, rW = 0.14, nCoils = 10. Κάθε δακτύλιος (ring) έχει 4
κόμβους σε τομή 2×2 στο (y,z) με offset ±q/2 (q = 0.045, «πάχος συρμού»).

## 3. Τοπολογία ελατηρίων

Ανά nSegments δακτυλίους (24–96, default 48):

| Κλάση | Σύνδεση | Ρόλος |
|---|---|---|
| 0 axial | (x,n)–(x+1,n) κατά την έλικα | διάδοση κυμάτων στον 4D σωλήνα |
| 1 ring side | πλευρές τετραγώνου yz | ακαμψία τομής |
| 2 ring diag | διαγώνιοι τετραγώνου yz | αντίductors στρέψης τομής |
| 3 winding | (x,n)–(x+1,n) στον άξονα w | η «4D περιέλιξη» (παράμετρος Helix) |
| 4 shear | (x,n)–(x+1,n±1) yz-flip | διατμητική ακαμψία |
| 5 drive | μάζα εισόδου–ring 0 | transducer εισόδου |

Σύνολο: 18·N−8 ελατήρια. Μικρού τраст.tm (small-strain) ελατήριο σταθερής
διεύθυνσης n̂ (μοναδιαίο, προ-υπολογισμένο από τις θέσεις ηρεμίας) και
μήκους ηρεμίας L₀:

    e = (xⱼ − xᵢ)·n̂ − L₀
    f = k·e + c·(vⱼ − vᵢ)·n̂
    Fⱼ = −f·n̂ ,  Fᵢ = +f·n̂

Καμία sqrt ανά δείγμα — όλα προ-υπολογισμένα, ο βρόχος είναι SoA-friendly.

## 4. Ολοκλήρωση & ευστάθεια

Συμπλεκτικός (semi-implicit) Euler στο sample rate του host:

    v ← (v + dt·F/m)·g ,   x ← x + dt·v

Προϋπολογισμός CFL: η χειρότερη «frustrated» συνιστώσα βλέπει ΔΙΠΛΙΟ
άθροισμα στιβαρότητας ανά κόμβο (φράγμα Gershgorin): ω_max² ≈ 2·3.7·kAx,
άρα kAx = (1.25/dt)² / 7.4 ώστε dt·ω_max ≤ 1.25 < 2 (όριο symplectic
Euler). Η παράμετρος Tension κλιμακώνει το kAx κατά 0.40–1.35
(dt·ω_max ≤ ~1.45). Αγκύρεις 1.5 Hz (k = 88.8) προς τη θέση ηρεμίας
δεσμεύουν το οιονεί στατικό drift.

Κρίσιμο αριθμητικό σημείο: το `pos_` αποθηκεύει ΜΕΤΑΤΟΠΙΣΗ από τη θέση
ηρεμίας (u = x − x_rest), όχι απόλυτη θέση. Η επιμήκυνση υπολογίζεται ως
e = dot(u_j − u_i, n̂) χωρίς αφαίρεση μεγάλων σταθερών (dot ≈ 0.36, e ≈
1e-6, k ≈ 4e8) — με απόλυτες θέσεις η καταστροφική απαλοιφή στο float
δημιουργούσε δάπεδο θορύβου δύναμης που «άντλες» τα modes και εμπόδιζε
την αποσύνθεση της ουράς.

## 5. Απώλειες

- **Υλική υστέρηση:** ιξώδης απόσβεση ανά ελατήριο, ζ = 0.0003 (σχετικός
  ρυθμός απόσβεσης ζ·ω — ισχυρό HF rolloff, ουδέτερο στα βάθη).
- **RT60:** ανά-κόμβο g = exp(−dt/τ), τ = Decay/6.9078 (πτώση −60 dB).
- **Transducer εισόδου:** μάζα m = 4 με δική της γρήγορη απόσβεση (τ = 1.2 ms).

## 6. Transducers & pickups

- **Είσοδος:** AC-coupled (DC blocker 20 Hz — σιγουρα για το ελεύθερο άκρο)
  και δύναμη F = tanh(1.7·in)·4e6·Drive στη μάζα εισόδου (soft clip),
  συν προαιρετική έγχυση στον `w` (0.35·Helix) — «στρεβλώνει» την ώθηση.
- **Pickup A (L):** μέσες ταχύτητες του προτελευταίου δακτυλίου.
- **Pickup B (R):** ίδιο σε κινούμενη θέση (Pickup Pos, 45%–95% του μήκους).
  Η ταχύτητα `w` αναμιγνύεται με +ημίτονο στο L και −ημίτονο στο R:
  στερεοφωνική αποσυσχέτιση με διαφορετικό «chirp» ανά κανάλι.
- Κάθε pickup: one-pole LPF (700 Hz–12 kHz, Brightness) + DC blocker (~24 Hz).
- **Anti-Feedback (wet path):** 8-ζωνικό complementary octave crossover
  (160 Hz–10.24 kHz)· ανά ζώνη fast peak envelope vs slow rolling reference
  (900 ms) + απόλυτο ορίο· υπερβάσεις (συντονισμοί/feedback στις συχνότητες
  που διεγείρει η είσοδος) καταστέλλονται αναλογικά (fast attack 1.5 ms,
  release 320 ms, max cut −21 dB) — η στάθμη παραμένει σταθερή σε όλες τις
  συχνότητες. Στο 0: πλήρως διαφανές (άθροισμα ζωνών == είσοδος).

## 7. Χαρτογράφηση παραμέτρων

| Πάραμ. | Εύρος | Επίδραση |
|---|---|---|
| Drive | 0–1 | gain + saturation εισόδου |
| Tension | 0–1 | kAx → ταχύτητα κυμάτων, φωτεινότητα |
| Decay | 0.4–12 s | RT60 |
| 4D Coupling | 0–1 | k των winding ελατηρίων (0.04–0.22·kAx) + έγχυση w |
| Pickup Pos | 0–1 | θέση 2ου pickup |
| Brightness | 0–1 | cutoff pickups |
| Spring Size | 24–96 | nSegments (και CPU) |
| Mix / Output | 0–1 / ±dB | mix, τελικό gain |

## 8. Υλοποίηση & έλεγχος

- `Source/SpringLSM.h`: engine header-only (C++17, χωρίς JUCE). SoA πίνακες
  (component-major) για vectorization, pre-allocated για max μέγεθος.
- `Test/SpringTest.cpp`: impulse (ευστάθεια/απόσβεση), noise burst (επίπεδα,
  CPU), sine, corner sweep (tension/helix/decay/96 τμήματα, drive=1).
- Προστασία: ανά-δείγμα isfinite guard → reset σε ενδεχέμενο blow-up.

## 9. Περιορισμοί / ιδέες συνέχειας

- Το μοντέλο είναι γραμμικό μετά το tanh της εισόδου (κανένα tension-dependent
  pitch shift· μπορεί να προστεθεί ως εξάρτηση k(Drive)).
- CPU ανάλογο του Spring Size· πιθανή SIMD/NNE βελτιστοποίηση του βρόχου
  ελατηρίων, ή group-excitation ανά 2 δείγματα για υπερδειγματοληψία.
- Δεν υπάρχει μή-γραμμική «fuzz» του συρμού (contact nonlinearity) — εύκολη
  επέκταση στα axial ελατήρια (cubic term).
